<%= form_for(status_payment) do |f| %>
  <% if status_payment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(status_payment.errors.count, "error") %> prohibited this status_payment from being saved:</h2>

      <ul>
      <% status_payment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
